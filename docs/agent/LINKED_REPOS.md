# Linked Repos

## `landing-inglesconliza-com`

- Owns the root `www.inglesconliza.com` landing Worker.
- The whiteboard Worker owns the more specific `www.inglesconliza.com/whiteboard*` route.

## `inglesconliza-manager`

- Tracks the Telegram topic and fleet membership for this fork.
